apiVersion: v1
kind: ConfigMap
metadata:
  name: test-configmap2
  namespace: default
data:
  app.properties: |
    environment=development
    log.level=debug
  config.json: |
    {
      "api": {
        "endpoint": "https://api.examples.com",
        "timeout": 30
      }
    }
